using UnityEngine;

/// <summary>
/// [DESHABILITADO] - Reemplazado por el sistema de Animator moderno en PlayerController
/// Este script causaba conflictos con las animaciones. Usar el Animator Controller moderno en su lugar.
/// </summary>
public class PlayerAnimationHandler : MonoBehaviour
{
    // Todas las variables y métodos han sido removidos - este script está deshabilitado
    // Las animaciones ahora se controlan desde PlayerController.cs que usa un Animator moderno

    void Start()
    {
        // DESHABILITADO - Las animaciones ahora se controlan desde PlayerController.cs
    }

    void Update()
    {
        // DESHABILITADO - Las animaciones ahora se controlan desde PlayerController.cs
    }
}
